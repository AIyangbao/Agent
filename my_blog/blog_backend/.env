# my_blog 容器部署环境变量（docker compose 自动读取）
# 注意：.env 已在 .dockerignore 中排除，不会进入镜像
DB_PWD=15358810yang
DB_NAME=blog_db
JWT_PWD=15358810
# AI 模块密钥：暂留空，等你提供真实 DASHSCOPE_API_KEY 后填入并重启 backend
DASHSCOPE_API_KEY=sk-933db338e50c42fd82ab226939d7b5b4
QWEN_MODEL=deepseek-v4-flash

# 本地直连用（docker 部署时 compose 已硬编码 DB_HOST=mysql，不冲突）
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
